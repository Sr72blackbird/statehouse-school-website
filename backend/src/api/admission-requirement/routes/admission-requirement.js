'use strict';

/**
 * admission-requirement router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::admission-requirement.admission-requirement');
