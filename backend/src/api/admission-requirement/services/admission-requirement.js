'use strict';

/**
 * admission-requirement service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::admission-requirement.admission-requirement');
